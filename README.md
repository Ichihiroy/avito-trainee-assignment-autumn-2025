# Система управления объявлениями для модерации

Веб-приложение для модерации объявлений на платформе Авито. Реализовано в рамках тестового задания для стажёра Frontend (осенняя волна 2025).

## 🚀 Технологии

### Frontend

- **React 18** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript для надежности кода
- **Vite** - быстрая система сборки
- **React Router DOM** - маршрутизация
- **Axios** - HTTP-клиент для API запросов
- **Chart.js + react-chartjs-2** - визуализация статистики
- **Lucide React** - современные SVG иконки

### Backend

- **Node.js + Express** - серверная часть
- **API v1** - RESTful API для работы с объявлениями

## 📋 Функциональность

### Реализованные возможности

#### ✅ Основные функции

- **Список объявлений** (`/list`)

  - Карточки объявлений с изображением, ценой, категорией
  - Фильтрация по статусу, категории, диапазону цен
  - Поиск по названию
  - Сортировка по дате, цене, приоритету
  - Пагинация (10 объявлений на страницу)
  - Синхронизация фильтров с URL

- **Детальный просмотр** (`/item/:id`)

  - Галерея изображений
  - Полная информация об объявлении
  - Информация о продавце
  - История модерации
  - Панель действий модератора (одобрить/отклонить/на доработку)
  - Навигация между объявлениями

- **Статистика** (`/stats`)
  - Карточки с метриками
  - График активности по дням
  - Круговая диаграмма решений
  - График по категориям
  - Фильтр по периоду (сегодня/неделя/месяц)

#### ⭐ Дополнительные возможности

- **Горячие клавиши**

  - `A` - одобрить объявление
  - `D` - отклонить объявление
  - `→` - следующее объявление
  - `←` - предыдущее объявление
  - `/` - фокус на поиск

- **Bulk-операции**

  - Выбор нескольких объявлений
  - Массовое одобрение/отклонение

- **Тёмная тема**

  - Переключатель темы
  - Сохранение в localStorage

- **Адаптивный дизайн**
  - Полностью responsive для мобильных устройств
  - Плавные анимации
  - Современный UI

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js v20 или выше
- npm или yarn

### Локальный запуск

#### 1. Запуск сервера

```bash
cd tech-int3-server
npm install
npm start
```

Сервер запустится на порту 3001.

#### 2. Запуск клиента

```bash
cd tech-int3-client
npm install
npm run dev
```

Клиент запустится на порту 3000.

Приложение будет доступно по адресу: `http://localhost:3000`

### Запуск с Docker Compose

```bash
docker-compose up
```

Сервисы будут доступны:

- Frontend: `http://localhost:3000`
- Backend API: `http://localhost:3001`

## 📁 Структура проекта

```
tech-int3-client/
├── src/
│   ├── components/        # Переиспользуемые компоненты
│   │   ├── AdCard.tsx     # Карточка объявления
│   │   ├── Filters.tsx    # Компонент фильтров
│   │   ├── Pagination.tsx # Пагинация
│   │   ├── Loading.tsx    # Индикатор загрузки
│   │   └── Layout.tsx     # Основной layout
│   ├── pages/             # Страницы приложения
│   │   ├── AdsList.tsx    # Список объявлений
│   │   ├── AdDetail.tsx   # Детальный просмотр
│   │   └── Stats.tsx      # Статистика
│   ├── services/          # API сервисы
│   │   └── api.ts         # Axios конфигурация и эндпоинты
│   ├── types/             # TypeScript типы
│   │   └── api.ts         # Типы из API схемы
│   ├── contexts/          # React контексты
│   │   └── ThemeContext.tsx # Контекст темы
│   ├── App.tsx            # Главный компонент
│   ├── main.tsx           # Точка входа
│   └── index.css          # Глобальные стили
├── package.json
├── tsconfig.json
├── vite.config.ts
└── Dockerfile

tech-int3-server/
├── src/
│   ├── controllers/       # Контроллеры API
│   ├── routes/            # Маршруты
│   ├── models/            # Модели данных
│   └── middleware/        # Middleware
├── server.js
├── schema.yaml            # OpenAPI спецификация
└── package.json
```

## 🎨 Дизайн

Приложение использует современный, минималистичный дизайн с акцентом на удобство использования:

- **Цветовая схема**: Синий (primary), зелёный (success), красный (danger), жёлтый (warning)
- **Светлая/тёмная тема**: Автоматическое переключение с сохранением предпочтений
- **Анимации**: Плавные переходы и hover-эффекты
- **Типографика**: System fonts для быстрой загрузки
- **Отзывчивость**: Mobile-first подход

## 🔑 API Endpoints

### Объявления

- `GET /api/v1/ads` - Список объявлений с фильтрацией
- `GET /api/v1/ads/:id` - Детали объявления
- `POST /api/v1/ads/:id/approve` - Одобрить объявление
- `POST /api/v1/ads/:id/reject` - Отклонить объявление
- `POST /api/v1/ads/:id/request-changes` - Запросить изменения

### Статистика

- `GET /api/v1/stats/summary` - Общая статистика
- `GET /api/v1/stats/chart/activity` - График активности
- `GET /api/v1/stats/chart/decisions` - График решений
- `GET /api/v1/stats/chart/categories` - График категорий

### Модератор

- `GET /api/v1/moderators/me` - Информация о текущем модераторе

## 🧪 Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для production

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

## 🌟 Особенности реализации

### Архитектура

- **Компонентный подход**: Разделение на переиспользуемые компоненты
- **TypeScript**: Полная типизация для безопасности
- **Service Layer**: Выделенный слой для работы с API
- **Context API**: Управление глобальным состоянием (тема)

### Производительность

- **Lazy Loading**: Оптимизация загрузки компонентов
- **Memoization**: Оптимизация ре-рендеров
- **Debounce**: Для поисковых запросов
- **Pagination**: Ограничение количества данных

### UX/UI

- **Keyboard Navigation**: Горячие клавиши для быстрой работы
- **Loading States**: Индикаторы загрузки
- **Error Handling**: Обработка ошибок с понятными сообщениями
- **Responsive Design**: Адаптация под все размеры экранов
- **Accessibility**: Семантическая разметка, ARIA-атрибуты

## 📝 Обоснование выбора технологий

### Vite

Выбран за скорость разработки и сборки. Hot Module Replacement (HMR) значительно ускоряет разработку по сравнению с Webpack.

### TypeScript

Обеспечивает типобезопасность, автодополнение в IDE и раннее обнаружение ошибок. Критично для больших проектов.

### Axios

Более удобный API по сравнению с fetch, встроенная поддержка interceptors, автоматическая сериализация данных.

### Chart.js

Популярная библиотека визуализации данных с хорошей документацией и поддержкой React через react-chartjs-2.

### Lucide React

Современная, легковесная библиотека иконок с отличным tree-shaking и TypeScript поддержкой.

Тестовое задание для стажёрской программы Авито (осень 2025)
